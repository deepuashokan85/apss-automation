---
- name:                Creation of simple storage s3
  hosts:               localhost
  gather_facts:        False

  vars:
    region:            us-east-2
    state:             present
    name_ram:          rambucket
  tasks:

    - name:            final task for S3 Bucket
      s3_bucket:
        name:          "{{ name_ram }}"
        region:        "{{ region }}"
        state:         "{{ state }}"
