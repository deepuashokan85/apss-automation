- name: Creating AMI
  vars:
    instance_id: i-0d4cc9909c5480e11
    instance_name: ramami
    region: us-east-2

  tasks:
    - ec2_ami:
        instance_id: "{{ instance_id }}"
        region: "{{ region }}"
        instance_name: "{{ instance_name }}"
        wait: yes
        state: present
